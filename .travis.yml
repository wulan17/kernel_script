language: C
sudo: required
before_install:
  - sudo apt-get update
  - sudo apt-get install -y bc bison build-essential curl flex g++-multilib gcc-multilib gnupg gperf imagemagick lib32ncurses5-dev lib32readline-dev lib32z1-dev libesd0-dev liblz4-tool libncurses5-dev libsdl1.2-dev libssl-dev libwxgtk3.0-dev libxml2 libxml2-utils lzop pngcrush rsync schedtool squashfs-tools xsltproc yasm zlib1g-dev
before_script:
  - cd $HOME && PATH=~/bin:$PATH
  - curl https://storage.googleapis.com/git-repo-downloads/repo > ~/bin/repo
  - chmod a+x ~/bin/repo
script:
  - cd $HOME && git clone -b stock-8.0  https://github.com/iamsaalim/android_kernel_X00PD.git kernel
  - cd $HOME && git clone https://github.com/iamsaalim/kernel_script build_kernel
  - cp $HOME/build_kernel/build.sh $HOME/kernel/build.sh
  - cd $HOME/kernel && chmod +x build.sh
  - |
    cd $HOME/kernel
    source build.sh
    make -j4 O=out
after_success:
  - cp $HOME/kernel/out/arch/arm64/boot/Image.gz-dtb $HOME/kernel/zImage
deploy:
  skip_cleanup: true
  provider: releases
  api_key: "$GITHUB_TOKEN"
  file_glob: true
  file: $HOME/kernel/zImage
  on:
    tags: false
    repo: iamsaalim/kernel_script
    branch: master
branches:
  except:
    - /^(?i:untagged)-.*$/
